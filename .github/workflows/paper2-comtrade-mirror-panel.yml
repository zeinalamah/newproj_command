name: Paper 2 global UN Comtrade mirror-export panel
on:
  pull_request:
    branches: [main]
    paths:
      - 'paper2/collect_comtrade_mirror_exports.py'
      - '.github/workflows/paper2-comtrade-mirror-panel.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --upgrade pandas numpy requests
      - run: python paper2/collect_comtrade_mirror_exports.py
      - if: always()
        run: |
          cat output/comtrade_mirror_exports/comtrade_mirror_export_summary.json || true
          tail -20 output/comtrade_mirror_exports/comtrade_mirror_request_audit.csv || true
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: paper2-global-comtrade-mirror-exports-2019-2026
          path: output/comtrade_mirror_exports
          retention-days: 7
          if-no-files-found: error
